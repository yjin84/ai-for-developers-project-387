import "@typespec/http";
import "./models.tsp";
import "./event-types.tsp";
import "./slots.tsp";
import "./bookings.tsp";

using Http;

/**
 * Сервис бронирования встреч по календарю.
 *
 * В системе нет регистрации и авторизации:
 * - Владелец календаря — один заранее заданный профиль, используемый по
 *   умолчанию в админской части (создание типов событий, просмотр
 *   предстоящих встреч).
 * - Гость бронирует слоты без создания аккаунта и без входа в систему
 *   (просмотр видов брони, выбор свободного слота, создание бронирования).
 */
@service(#{ title: "Calendar Booking Service" })
namespace CalendarBooking;
