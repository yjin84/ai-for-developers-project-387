# Сборка для E2E (vite build --mode test): включает MSW-воркер и направляет
# запросы API на адрес, который перехватывает service worker.
# Управляется воркфлоу CI и локальным запуском `npm run test:e2e`.
VITE_ENABLE_MSW=true
VITE_API_BASE_URL=http://api.test