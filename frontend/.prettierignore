# Автогенерируемые файлы — форматируются инструментом-генератором, не Prettier.
src/api/schema.d.ts

# Генерируется `npx msw init` (генерируется MSW, не форматируем).
public/mockServiceWorker.js

# Генерируется тестовым прогоном покрытия.
coverage
